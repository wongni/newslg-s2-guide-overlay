"use client";

// This component is no longer needed - login prompt is built into GuideSourceSelector.
// Kept as empty export to avoid import errors from other files.

export function LoginPromptBanner() {
  return null;
}
